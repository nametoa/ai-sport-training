# COROS Team API credentials
# Copy this file to secrets.toml for local development,
# or paste the [coros] section into Streamlit Cloud → Settings → Secrets.
#
# To find these values:
# 1. Log in to https://t.coros.com
# 2. Open browser DevTools → Network tab
# 3. Look at any API request to teamcnapi.coros.com
# 4. Copy the accesstoken header and cookie values

[coros]
access_token = "YOUR_COROS_ACCESS_TOKEN"
user_id = "YOUR_COROS_USER_ID"
cookie_wbkfro = "YOUR_COOKIE_WBKFRO_VALUE"
cookie_region = "2"
base_url = "https://teamcnapi.coros.com"
